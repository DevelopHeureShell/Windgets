<Window x:Class="Windgets.w_pic"
       xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Windgets"
        mc:Ignorable="d"
        Title="Windgets : Picture" Height="219.395" Width="414.057" Background="{x:Null}" MouseDown="Window_MouseDown" WindowStyle="None" AllowsTransparency="True" ResizeMode="CanResizeWithGrip" Icon="icon.png"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
     TextElement.Foreground="{DynamicResource MaterialDesignBody}"
     TextElement.FontWeight="Regular"
     TextElement.FontSize="13"
     TextOptions.TextFormattingMode="Ideal"
     TextOptions.TextRenderingMode="Auto"
     FontFamily="{DynamicResource MaterialDesignFont}">
    <Grid>
        <Grid.ContextMenu>
            <ContextMenu>

                <MenuItem Header="Source"  Click="source"/>
                <MenuItem Header="Fermer"  Click="fermer"/>
            </ContextMenu>
        </Grid.ContextMenu>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Image x:Name="Pic"  Grid.Column="1" HorizontalAlignment="center"  VerticalAlignment="center"  Source="blank.png" Stretch="UniformToFill"/>

    </Grid>
</Window>
